## HALLUCINATION DETECTION LOG Entry # (from Sheet 2) Hallucinati on Type AI's Claim Reality Check How Detected Corrective Action 1 Oversimpli fication AI asserts that connecting the 5V TX pin of Arduino directly to the 3.3V RX pin of ESP8266 via jumping wire is completely safe. A 5V logic signal sent directly into a 3.3V tolerant pin will slowly degrade or instantly fry the ESP8266 chip due to overvoltage. Cross-referencing the ESP8266 hardware datasheet regarding maximum input voltage tolerance on GPIO pins. Rejected direct wiring. Designed and implemented a hardware voltage divider circuit using 220Ω and 330Ω resistors to safely step down the signal to 3.3V. 6 Logic Error AI claimed that the Arduino freezes because the loop is running too fast, and suggested adding a delay(100) at the end to fix it. delay() does not fix the root cause. The freezing is caused by the pulseIn() function waiting indefinitely (up to 1 second) for an echo when the sensor points to an open sky. Code tracing and simulating an "open lid" scenario by pointing the sensor to the ceiling, observing the Serial Monitor output suddenly lagging. Removed the delay() and added a strict timeout parameter of 30,000 microseconds to the pulseIn() function to force it to be non- blocking. 8 Context Misunders tanding AI suggested using a JavaScript alert("Bin is full!") function to warn users on the Web Dashboard. The alert() function is synchronous and completely blocks the browser's main thread, stopping the real- time WebSocket MQTT data stream until the user clicks OK. Running the frontend code and noticing that background data updates (gas, humidity) stopped entirely whenever the pop-up appeared. Replaced the blocking alert() with smooth DOM manipulation (changing CSS Tailwind classes to turn the background red and adding a pulsing text effect). 10 Context Misunders tanding AI suggested powering the HC-SR04 ultrasonic sensor by connecting its VCC pin to the 3V3 power output of the ESP8266 NodeMCU. The standard HC-SR04 requires a stable 5V power supply. Powering it with 3.3V causes erratic distance readings, noise, or total hardware failure. Reading the HC-SR04 hardware specifications and observing unstable distance readings during physical breadboard testing. Re-routed the VCC pin of the HC-SR04 sensor to the 5V output pin of the Arduino UNO to ensure sufficient power supply.